{{- if .Values.emailService.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: emailservice
  labels:
    {{- include "online-boutique.labels" . | nindent 4 }}
    app.kubernetes.io/name: emailservice
    app.kubernetes.io/component: email
spec:
  replicas: {{ .Values.emailService.replicaCount }}
  selector:
    matchLabels:
      app.kubernetes.io/name: emailservice
  template:
    metadata:
      labels:
        app.kubernetes.io/name: emailservice
        app.kubernetes.io/component: email
        app.kubernetes.io/part-of: online-boutique
    spec:
      serviceAccountName: emailservice
      terminationGracePeriodSeconds: 5
      securityContext:
        fsGroup: 1000
        runAsGroup: 1000
        runAsNonRoot: true
        runAsUser: 1000
      containers:
        - name: emailservice
          image: "{{ .Values.global.imageRepository }}/{{ .Values.emailService.image.name }}:{{ .Values.global.imageTag }}"
          imagePullPolicy: {{ .Values.global.imagePullPolicy }}
          securityContext:
            allowPrivilegeEscalation: false
            privileged: false
            readOnlyRootFilesystem: true
            capabilities:
              drop:
                - ALL
          ports:
            - name: grpc
              containerPort: {{ .Values.emailService.containerPort }}
              protocol: TCP
          env:
            - name: PORT
              value: {{ .Values.emailService.containerPort | quote }}
            - name: DISABLE_PROFILER
              value: "1"
          readinessProbe:
            periodSeconds: {{ .Values.emailService.probes.readiness.periodSeconds }}
            grpc:
              port: {{ .Values.emailService.containerPort }}
          livenessProbe:
            periodSeconds: {{ .Values.emailService.probes.liveness.periodSeconds }}
            grpc:
              port: {{ .Values.emailService.containerPort }}
          resources:
            {{- toYaml .Values.emailService.resources | nindent 12 }}
{{- end }}
